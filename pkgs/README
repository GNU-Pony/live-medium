These .scroll files are instructions for spike to build and package the software
packages needed for a functional system. They will, each, generate a .pkg.tar.xz
that is a spooled package to install to the live medium, and a directory that is
used to generate the .pkg.tar.xz file.
